# RePaint configuration for OAI dataset testing (RESTORED WORKING VERSION)
# Based on the backup configuration that actually produced output successfully
# This is the exact configuration that generated 25 inpainted images in the backup

attention_resolutions: 32,16,8
class_cond: false
diffusion_steps: 1000
learn_sigma: true
noise_schedule: linear
num_channels: 256
num_head_channels: 64
num_heads: 4
num_res_blocks: 2
resblock_updown: true
use_fp16: false
use_scale_shift_norm: true
classifier_scale: 4.0
lr_kernel_n_std: 2
num_samples: 22  # Number of test images (full dataset)
show_progress: true
timestep_respacing: '25'  # Very fast: 25 steps instead of 250
use_kl: false
predict_xstart: false
rescale_timesteps: false
rescale_learned_sigmas: false
classifier_use_fp16: false
classifier_width: 128
classifier_depth: 2
classifier_attention_resolutions: 32,16,8
classifier_use_scale_shift_norm: true
classifier_resblock_updown: true
classifier_pool: attention
num_heads_upsample: -1
channel_mult: ''
dropout: 0.0
use_checkpoint: false
use_new_attention_order: false
clip_denoised: true
use_ddim: false  # DDIM not available, use regular sampling with fewer steps
latex_name: RePaint
method_name: Repaint
image_size: 256
model_path: ./data/pretrained/places256_300000.pt  # Using Places model for general images
name: oai_test_restored
inpa_inj_sched_prev: true
n_jobs: 1
print_estimated_vars: true
inpa_inj_sched_prev_cumnoise: false
schedule_jump_params:
  t_T: 25  # Match timestep_respacing
  n_sample: 1
  jump_length: 3  # Very reduced for faster processing
  jump_n_sample: 3  # Very reduced for faster processing
data:
  eval:
    oai_test_restored:
      mask_loader: true
      gt_path: ../OAI_dataset/test/img
      mask_path: ../OAI_dataset/test/mask_inv
      image_size: 256
      class_cond: false
      deterministic: true
      random_crop: false
      random_flip: false
      return_dict: true
      drop_last: false
      batch_size: 1
      return_dataloader: true
      offset: 0
      max_len: 22  # Number of test images
      paths:
        srs: ../OAI_dataset/output/RePaint_Places/inpainted
        lrs: ../OAI_dataset/output/RePaint_Places/gt_masked
        gts: ../OAI_dataset/output/RePaint_Places/gt
        gt_keep_masks: ../OAI_dataset/output/RePaint_Places/gt_keep_mask
